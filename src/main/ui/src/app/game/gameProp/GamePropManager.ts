import {GameProp} from './GameProp';
import {MasterRole} from '../role/MasterRole';

export class GamePropManager {

  /**
   * 爆装备: 从怪物中获取道具
   * @param enemies 怪物列表
   */
  static gatherGameProp(enemies: MasterRole[]): GameProp[] {

    return [];
  }
}
